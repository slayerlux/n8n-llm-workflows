# n8n Configuration
N8N_URL=http://localhost:5678

# n8n Authentication (choose one)
# Option 1: API Key (recommended for automation)
N8N_API_KEY=your-api-key-here

# Option 2: Session Cookie (from browser after logging in)
# N8N_SESSION_COOKIE=n8n-auth-token=your-token-here

# OpenAI-compatible LLM Backend (already in docker-compose.yml)
OPENAI_API_BASE=http://host.docker.internal:1234/v1
OPENAI_API_KEY=lm-studio
OPENAI_MODEL=openai/gpt-oss-20b

